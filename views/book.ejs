<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ZhanCinema - Бронирование</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
</head>
<body class="bg-gray-100 p-6">
  <header class="mb-6">
    <h1 class="text-3xl font-bold text-center text-blue-600">ZhanCinema</h1>
  </header>
  <h2 class="text-2xl font-bold mb-4">Бронирование билета - <%= movie.title %></h2>
  <form method="POST" action="/book" class="space-y-4">
    <input type="hidden" name="movie_id" value="<%= movie.id %>">
    <label class="block">
      Место:
      <select name="seat" class="border p-2 w-full" required>
        <% for (let row of ['A', 'B', 'C']) { for (let num = 1; num <= 5; num++) { %>
          <option><%= row + num %></option>
        <% }} %>
      </select>
    </label>
    <button type="submit" class="bg-green-500 text-white px-4 py-2 rounded">Забронировать</button>
  </form>
</body>
</html>
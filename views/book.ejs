<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Бронирование - ZhanCinema</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800 p-4">
  <header class="text-center mb-6">
    <h1 class="text-3xl font-bold text-blue-600">ZhanCinema</h1>
  </header>

  <div class="max-w-2xl mx-auto bg-white p-4 rounded shadow">
    <h2 class="text-xl font-semibold mb-4">Фильм: <%= movie.title %> — <%= time %></h2>
    <img src="/images/hall.jpg" alt="Зал" class="w-full mb-4 rounded" />

    <form method="POST" action="/book" class="space-y-4">
      <input type="hidden" name="movie_id" value="<%= movie.id %>">
      <input type="hidden" name="time" value="<%= time %>">
      <label class="block">Выберите место:
        <select name="seat" class="border p-2 w-full mt-1" required>
          <% for (let row of ['A', 'B', 'C']) { for (let num = 1; num <= 5; num++) { %>
            <option><%= row + num %></option>
          <% }} %>
        </select>
      </label>
      <button type="submit" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">Забронировать</button>
    </form>
  </div>
</body>
</html>
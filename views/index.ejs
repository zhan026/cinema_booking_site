<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ZhanCinema</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800 p-4">
  <header class="text-center mb-6">
    <h1 class="text-4xl font-bold text-blue-600">ZhanCinema</h1>
  </header>

  <form method="GET" action="/" class="mb-6 text-center">
	<select name="date" onchange="this.form.submit()" class="p-2 border rounded">
	  <option value="сегодня" <%= selectedDate === 'сегодня' ? 'selected' : '' %>>Сегодня</option>
	  <option value="завтра" <%= selectedDate === 'завтра' ? 'selected' : '' %>>Завтра</option>
	  <option value="послезавтра" <%= selectedDate === 'послезавтра' ? 'selected' : '' %>>Послезавтра</option>
	</select>
  </form>

  <% movies.forEach(movie => { %>
    <div class="bg-white rounded shadow p-4 mb-4">
      <h2 class="text-2xl font-bold mb-2"><%= movie.title %></h2>
      <img src="/images/<%= movie.image %>" alt="<%= movie.title %>" class="w-full max-w-sm mx-auto mb-2 rounded" />
      <div class="text-sm text-gray-600 mb-1">Сеансы:</div>
      <div class="space-x-2">
        <% movie.times.forEach(time => { %>
          <a href="/book/<%= movie.id %>?time=<%= time %>" class="inline-block bg-blue-500 text-white px-3 py-1 rounded hover:bg-blue-700"><%= time %></a>
        <% }) %>
      </div>
    </div>
  <% }) %>
</body>
</html>